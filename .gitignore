# Dependencies
node_modules/

# Build output
out/
dist/
*.tsbuildinfo

# Extension packages
*.vsix

# VS Code extension testing
.vscode-test/
.vscode-test-web/

# Editor / IDE (launch.json is kept intentionally)
.vscode/settings.json
.vscode/tasks.json
.idea/
*.swp
*.swo

# Logs & diagnostics
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
coverage/
.nyc_output/

# Environment & secrets
.env
.env.*
!.env.example
!.env.test.example
*.pem
*.key
secrets.json

# OS files
.DS_Store
._*
Thumbs.db
desktop.ini

# Temp
tmp/
temp/
*.tmp
*.bak

# Optional local cache for the DuckDB integration tests — not required to run
# the suite (those tests skip themselves without it); populate with
# `npm install @duckdb/node-api --prefix .duckdb-test-cache` to enable them.
.duckdb-test-cache/
